import { resolveConfig } from "vle-editor/config";

/**
 * VLE's configuration — see node_modules/vle-editor/dist/config.d.ts for
 * every field. Only projectRoot is required; everything else below either
 * has a sensible default or is auto-detected. Uncomment and adjust
 * anything that doesn't fit your setup.
 */
export const vleConfig = resolveConfig({
  projectRoot: process.cwd(),

  // repoRoot defaults to the real git top-level (auto-detected via `git
  // rev-parse --show-toplevel`), not projectRoot — override only if that's
  // somehow wrong for your setup:
  // repoRoot: path.resolve(process.cwd(), ".."),

  // One paragraph describing your project's stack/structure/conventions —
  // handed to the agent before every task. The more specific this is, the
  // less the agent has to rediscover by grepping around on every request.
  // promptContext: "You are editing my-app, a Next.js app using the App Router, Tailwind CSS, and shadcn/ui components under components/ui/.",

  // Directories (relative to projectRoot) scanned for the design-system palette.
  // uiDirs: ["components/ui"],

  // accentColor: "#9b8ec4",

  // stylingMode defaults to "tailwind" if a tailwind.config.* exists here,
  // otherwise "inline" (real style={{...}} values — works regardless of
  // your CSS methodology). Override only if the auto-detection guesses wrong:
  // stylingMode: "inline",
});
