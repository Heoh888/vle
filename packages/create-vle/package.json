{
  "name": "create-vle",
  "version": "0.1.10",
  "description": "Scaffolds VLE (Visual Live Editor) into a Next.js App Router or Vite + React project.",
  "license": "MIT",
  "bin": {
    "create-vle": "./dist/index.js"
  },
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
